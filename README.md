# Simple flask api
The program uses python files to create a REST API that is containerized 
with docker to other files in bash and powershell to run the program.

Run script in windows
```
.\run.ps1
```
Run script in linux
```
./run
```
